//  Student routes 
//  Student Layout(with navbar)

export default function StudentLayout({ children }) {
  return (
    <html lang="en">
      <body
      >
        {children}
      </body>
    </html>
  );
}